# We intentionally don't use "hotplug" here,
# as there is a test to remove the bridge interface,
# and we want the interface still to work afterwards

BOOTPROTO='static'
NAME='Ethernet Card 1'

IPADDR='172.16.0.123/16'
IPADDR6='fd00:c0de:ba5e::123/48'

STARTMODE='auto'
USERCONTROL='no'
